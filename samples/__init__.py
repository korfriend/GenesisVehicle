"""Bundled SDK samples — see `samples/README.md` for the catalog.

Importable as modules so they can be run with `python -m`:

    python -m genesis_vehicle.samples.quickstart
    python -m genesis_vehicle.samples.slope_hold
    python -m genesis_vehicle.samples.batched_rollout
"""
