# Moved — see [`dual-scene-raycast.md`](dual-scene-raycast.md)

Renamed in v1.1.0 as part of the terminology unification: the subsystem is the
**wheel-raycast** (`raycast_mode`), with two modes — **`dual_scene`** (default)
and **`single_scene`**. "two-scene" in older notes/links ≡ `dual_scene`.
